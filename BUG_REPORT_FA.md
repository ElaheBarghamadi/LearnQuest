# 📋 گزارش بررسی پروژه LearnQuest

**تاریخ:** ۲۰۲۶-۰۸-۱۸ | **وضعیت نهایی:** ✅ بدون باگ مسدودکننده (۲ باگ واقعی پیدا و رفع شد)

---

## ۱) پروژه چیست؟

**LearnQuest** یک پلتفرم آموزشی **گیمیفای‌شده** برای یادگیری زبان انگلیسی (با تمرکز بر فارسیزبانها) است که با **Django 5.2 + Channels (daphne) + Python 3.13** ساخته شده و یک تجربهی «بازیگونه» کامل دارد: درسها در قالب «جهانها» (Worlds) و «فصلها» (Chapters) چیده شدهاند و کاربر با انجام درس، کوئیز، امتحان و بازی، **XP و سکه** میگیرد.

## ۲) بخشهای پروژه (۱۳ اپ Django)

| اپ | نقش |
|---|---|
| `user` | ثبتنام/ورود، OTP بازیابی رمز، پروفایل عمومی، کارت پروفایل API |
| `Home` | داشبورد اصلی (XP/سطح/استریک/سیزن/پت)، ویرایش پروفایل، راهنما |
| `language_academy` | هستهی اصلی: جهانها، درسها، کوئیز/امتحان، واژگان، دیالوگ، نوشتن، اصطلاحات، گواهینامه، مربی AI، **CMS مدیریت محتوا** (`/academy/manage/`) |
| `Game` | ۱۲ بازی HTML/JS (سودوکو، حافظه، مار، ۲۰۴۸، مینروب…) با ذخیره امتیاز و دستاورد |
| `language` | ۵ بازی کلمه (وصل کن، حدس، جورچین، دیکته، دوئل) |
| `economy` | کیف پول (سکه/الماس/XP)، دفتر تراکنش غیرقابلتغییر، جایزه روزانه، سیزنپس، پت، بوستر، رهبری |
| `shop` | فروشگاه ۵۷ محصول (قاب، تم، پت، بلیط، جعبه مرموز…)، خرید idempotent، تجهیز، مصرف |
| `Messenger` | پیامرسان واقعی **WebSocket** (رمزنگاری پیامها با Fernet)، گروهها، لینک دعوت، بلاک دوسویه |
| `blog` | وبلاگ داینامیک: بازدید +۱، لایک، کامنت بدون ریلود، سایدبار پربازدیدها |
| `panel` | پنل ادمین اختصاصی: آمار، مدیریت کاربران، تنظیم سکه/الماس/XP/آیتم |
| `ContactUs`, `programapp_module`, `educational_platform` | فرم تماس، ماشینحسابهای آموزشی، پلتفرم آموزشی (پوسته) |

**امکانات فنی:** WebSocket واقعی (daphne)، رمزنگاری پیام، دفتر تراکنش immutable با idempotency-key، ضد-فارم پاداش (سقف روزانه)، CSRF فعال در همهجا، سرور انجمن InMemory برای پیشنمایش.

## ۳) چه چیزهایی تست شد؟ (روی دیتابیس تمیز از صفر)

- ✅ `manage.py check` + `makemigrations --check` + `migrate` — بدون خطا (فقط هشدار امنیتی CKEditor 4 که مربوط به کتابخانهی خارجی است)
- ✅ **۱۴۱ URL** (ناشناس + لاگین + استاف + پنل ادمین) — همه ۲۰۰/۳۰۲ سالم
- ✅ **۲۸۲ چک عملکردی**: ثبتنام، لاگین، کامنت/لایک بلاگ، خرید/تجهیز/مصرف، idempotency خرید و پنل، ۱۷ ذخیره امتیاز بازی، ۵ بازی کلمه، جریان کامل کوئیز/امتحان (شروع → پاسخ → ثبت → نتیجه)، تعیین سطح اصطلاحات، مربی AI بدون کلید API (fallback)، پیامرسان (DM/گروه/دعوت/بلاک/ترک)، WebSocket واقعی (اتصال/پیام/رد ناشناس)، کیف پول/پت/سیزن، پنل ادمین، CMS (ساخت جهان/فصل/درس/کوئیز/سوال)
- ✅ اسکریپت E2E خود نویسنده: **۱۸۵ چک سبز** (موارد ❌ همگی بهخاطر این بود که اسکریپت با حساب غیرادمین به بخش ادمین میرفت و برای یک درس بهنام قدیمی که در seed فعلی نیست)
- ✅ `seed_data.py` سه بار متوالی — **idempotent**، بدون رکورد تکراری

## ۴) 🐛 باگهای پیدا و رفعشده

### باگ ۱ (واقعی، کرش ۵۰۰): ساخت فصل/درس تکراری در CMS
**قبل:** در `/academy/manage/chapters/create/` و `lessons/create/` اگر ادمین `order` تکراری وارد میکرد (یا فرم دوبار ارسال میشد)، `IntegrityError` (UNIQUE world+order / chapter+order) بهصورت **خطای 500** میترکید.
**بعد:** بررسی پیش از ذخیره + هندل `IntegrityError` با پیام خطای فارسی روی فیلد order (چک شد: تکراری → 200 با پیام، یکتا → 302 موفق).

### باگ ۲ (واقعی، UX): فرمهای CMS خطاهای اعتبارسنجی را نشان نمیدادند
**قبل:** تمپلیتهای فرم (جهان/فصل/درس/کوئیز/امتحان/واژگان/مدال/سوالات) `form.errors` را اصلاً رندر نمیکردند — هر خطای فرم بیسروصدا ناپدید میشد.
**بعد:** بلوک نمایش خطا با لیست `<li>` به ۹ تمپلیت اضافه شد.

### اصلاحات دیگر
- **README.md**: دستور daphne اشتباه بود (`learnquest.asgi` ← `Config.asgi`)، اسکریپت seed وجود نداشت (`seed_airport_chapter` ← `seed_data.py` و دستورات واقعی)، آدرس ادمین آکادمی غلط بود (`/admin-panel/` ← `/academy/manage/`)، و راهنمای `.env` با رفتار واقعی کد هماهنگ شد.
- **Config/settings.py**: `ALLOWED_HOSTS` در حالت DEBUG باز شد تا در محیطهای توسعه/پیشنمایش کار کند (در تولید با settings_prod بیاثر است).

## ۵) نکات (باگ نیست، ولی بدانید)

- **تستهای `tests.py` همگی خالیاند** — ادعای «۶۹۰ تست» در README_FIRST مربوط به نسخههای قبلی است؛ در این ریپو تست خودکار واقعی (که من هم نوشتم) در `scripts/` است.
- **بدون کلید OpenRouter** مربی AI با بانک سوال/پاسخ داخلی کار میکند (تست شد). برای AI واقعی: `OPENROUTER_API_KEY` را ست کنید یا فایل `openrouter_key.txt` بسازید.
- دیتای seed فعلی درس «ویژه» (exclusive) ندارد؛ برای تست آن باید یک درس exclusive و محصول متناظر بسازید (من برای تست ساختم و گیت درست کار کرد).
- اسکریپتهای قدیمی نویسنده (`test_e2e.py`, `score_check.py`) به نام درسها/کاربرانی ارجاع میدهند که در seed فعلی نیستند — با `testuser`/`friend` تا ۱۸۵ چک سبز پاس میدهند.

## ۶) کاربران تست (فقط در این محیط)

| کاربر | رمز | دسترسی |
|---|---|---|
| `admin` | `Admin@12345` | سوپرادمین (پنل + CMS) |
| `ali` | `Test@12345` | کاربر عادی با ۵۰۰۰ سکه |
| `testuser` / `friend` | `test123456` | کاربران اسکریپتهای E2E |

## ۷) اجرا

```bash
pip install -r requirements.txt
python manage.py migrate
python seed_data.py            # محتوای آکادمی
python manage.py seed_economy && python manage.py seed_shop
python scripts/seed_blog.py
daphne -p 8000 Config.asgi:application   # ← اسم ماژول درست: Config (نه learnquest)
```

آدرسها: `/` (لندینگ) · `/home/` (داشبورد) · `/academy/` (آکادمی) · `/shop/` · `/messenger/` · `/blog/` · `/admin/` · `/academy/manage/` (CMS) · `/panel/` (پنل ادمین)
